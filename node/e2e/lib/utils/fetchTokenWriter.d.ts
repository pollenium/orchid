import { TokenWriter } from '../../../';
import { AccountNames, TokenNames } from '../fixtures';
export declare function fetchTokenWriter(accountName: AccountNames, tokenName: TokenNames): Promise<TokenWriter>;
